#include <stdio.h>
#include <string.h>  

int main(){
char A[100];
int i;

printf("Enter string:\n");
for (i = 0; scanf("%c", &A[i]) == 1 && A[i] != '\n'; ++i);

for (i = strlen(A) - 1; i >= 0; --i)
    printf("%c", A[i]);

return 0;
}
